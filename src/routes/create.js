// This module routes all creation requests.

// const router = require('./index').router;
//
// router.post('/:object([a-z]+)', (request, response) => {
//   require('../controllers').create(request.params.object, request.body, response);
// });
//
// router.post();
